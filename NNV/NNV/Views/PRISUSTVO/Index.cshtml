@model IEnumerable<NNV.Models.Prisustvo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Clanovi.Ime_i_prezime)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Sednice.Ucionica)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Prisutan)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Opravdano)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Clanovi.Ime_i_prezime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Sednice.Ucionica)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prisutan)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Opravdano)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
